spring:
  cloud:
    config:
      server:
        git:
          search-paths: respo
          uri: https://github.com/forezp/SpringcloudConfig/
          # username: �û��� # public ����Ҫ�û���������
          # password: ����
      label: master

  application:
    name: config-server
server:
  port: 8768
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

